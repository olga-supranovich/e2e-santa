{
  "name": "e2e-santa",
  "version": "1.0.0",
  "description": "",
  "main": "cypress.config.js",
  "scripts": {
    "cy:run": "cypress run",
    "clean:allure": "rm -R -f allure-report/* && rm -R -f allure-results/*",
    "generate:report": "allure serve allure-results",
    "run-tests:allure": "npm run clean:allure && npm run cy:run && npm run generate:report",
    "run:mocha": "cypress run --reporter mocha-junit-reporter",
    "jira-upload": "export token=$(curl -H \"Content-Type: application/json\" -X POST --data @\"keys.json\" https://xray.cloud.getxray.app/api/v2/authenticate| tr -d '\"') && curl -H \"Content-Type: text/xml\" -X POST -H \"Authorization: Bearer $token\"  --data @\"test-results.xml\" --url \"https://xray.cloud.getxray.app/api/v2/import/execution/junit?projectKey=AUTO\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^20.0.3",
    "@faker-js/faker": "^7.6.0",
    "cypress": "^13.7.1",
    "faker": "^5.5.3"
  },
  "devDependencies": {
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.0",
    "allure-commandline": "^2.28.0",
    "allure-cypress": "^2.15.1",
    "esbuild": "^0.20.2",
    "mocha-junit-reporter": "^2.2.1"
  }
}
